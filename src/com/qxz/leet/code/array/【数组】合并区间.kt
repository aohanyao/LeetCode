package com.qxz.leet.code.array

/**
 * 给出一个区间的集合，请合并所有重叠的区间。
 */
fun main() {

    /**
     * 输入: [[1,3],
     *       [2,6],
     *       [8,10],
     *       [15,18]]
     * 输出: [[1,6],[8,10],[15,18]]
     * 解释: 区间 [1,3] 和 [2,6] 重叠, 将它们合并为 [1,6].
     *
     * 输入: [[1,4],[4,5]]
     * 输出: [[1,5]]
     * 解释: 区间 [1,4] 和 [4,5] 可被视为重叠区间。
     *
     *  集合元素之间有一个是包含关系的即算重叠，求交集
     */
    fun merge(intervals: Array<IntArray>): Array<IntArray> {

        return arrayOf()
    }
}